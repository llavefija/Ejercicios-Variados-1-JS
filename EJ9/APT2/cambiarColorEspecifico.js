// Función para cambiar el color de fondo del documento
function cambiarColor(color) {
    document.body.style.backgroundColor = color; // Cambia el fondo del documento
}

// Función para cambiar el color del recuadro en la esquina
function cambiarRecuadroColor(color) {
    document.getElementById('recuadro').style.backgroundColor = color; // Cambia el color del recuadro
}
